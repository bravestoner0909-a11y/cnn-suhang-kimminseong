name: Supabase Keep Alive

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  keepalive:
    runs-on: ubuntu-latest

    steps:
      - name: Call Supabase test image
        run: |
          curl -L "https://zqdpfboagniulpqvukni.supabase.co/storage/v1/object/public/cnn-images/test.jpg"
